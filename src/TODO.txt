[ ] During model ctor, find model center and calculate radius R that encapsulates the sphere
    fully. Then store this in model and only ever render model when the Sphere is visible.

[ ] Make render modes cleaner instead of copy paste 6x times...

[ ] Look into DMA. Example in CPBoy src/cas/bootstrap.cpp